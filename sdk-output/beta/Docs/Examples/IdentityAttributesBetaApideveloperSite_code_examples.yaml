- path: /identity-attributes
  method: POST
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $identityAttributeBeta = @"
        
        "@
        
        # Create Identity Attribute
        
        try {
            $Result = ConvertFrom-JsonToidentityAttributeBeta -Json $identityAttributeBeta
            -BetaidentityAttributeBeta $Result
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityAttributeBeta $identityAttributeBeta  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-attributes/{name}
  method: DELETE
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $name =  # string | The attribute\'s technical name. (default to undefined)
        
        # Delete Identity Attribute
        
        try {
            -Betaname $name 
            
            # Below is a request that includes all optional parameters
            #  -Betaname $name  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-attributes/bulk-delete
  method: DELETE
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $identityAttributeNamesBeta = @"
        
        "@
        
        # Bulk delete Identity Attributes
        
        try {
            $Result = ConvertFrom-JsonToidentityAttributeNamesBeta -Json $identityAttributeNamesBeta
            -BetaidentityAttributeNamesBeta $Result
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityAttributeNamesBeta $identityAttributeNamesBeta  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-attributes/{name}
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $name =  # string | The attribute\'s technical name. (default to undefined)
        
        # Get Identity Attribute
        
        try {
            -Betaname $name 
            
            # Below is a request that includes all optional parameters
            #  -Betaname $name  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-attributes
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $includeSystem =  # boolean | Include \'system\' attributes in the response. (optional) (default to false)
        $includeSilent =  # boolean | Include \'silent\' attributes in the response. (optional) (default to false)
        $searchableOnly =  # boolean | Include only \'searchable\' attributes in the response. (optional) (default to false)
        $count =  # boolean | If *true* it will populate the *X-Total-Count* response header with the number of results that would be returned if *limit* and *offset* were ignored.  Since requesting a total count can have a performance impact, it is recommended not to send **count=true** if that value will not be used.  See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to false)
        
        # List Identity Attributes
        
        try {
            
            
            # Below is a request that includes all optional parameters
            #  -BetaincludeSystem $includeSystem -BetaincludeSilent $includeSilent -BetasearchableOnly $searchableOnly -Betacount $count  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-attributes/{name}
  method: PUT
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $name =  # string | The attribute\'s technical name. (default to undefined)
        $identityAttributeBeta = @"
        
        "@
        
        # Update Identity Attribute
        
        try {
            $Result = ConvertFrom-JsonToidentityAttributeBeta -Json $identityAttributeBeta
            -Betaname $name -BetaidentityAttributeBeta $Result
            
            # Below is a request that includes all optional parameters
            #  -Betaname $name -BetaidentityAttributeBeta $identityAttributeBeta  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
