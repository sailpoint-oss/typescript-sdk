- path: /identity-profiles
  method: POST
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $identityProfileBeta = @"
        
        "@
        
        # Create an Identity Profile
        
        try {
            $Result = ConvertFrom-JsonToidentityProfileBeta -Json $identityProfileBeta
            -BetaidentityProfileBeta $Result
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityProfileBeta $identityProfileBeta  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles/{identity-profile-id}
  method: DELETE
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $identityProfileId =  # string | The Identity Profile ID. (default to undefined)
        
        # Delete an Identity Profile
        
        try {
            -BetaidentityProfileId $identityProfileId 
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityProfileId $identityProfileId  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles/bulk-delete
  method: POST
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $requestBody =  # Array<string> | Identity Profile bulk delete request body.
        $requestBody = @""@ 
        
        
        # Delete Identity Profiles
        
        try {
            $Result = ConvertFrom-JsonTorequestBody -Json $requestBody
            -BetarequestBody $Result
            
            # Below is a request that includes all optional parameters
            #  -BetarequestBody $requestBody  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles/export
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $limit =  # number | Max number of results to return. See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to 250)
        $offset =  # number | Offset into the full result set. Usually specified with *limit* to paginate through the results. See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to 0)
        $count =  # boolean | If *true* it will populate the *X-Total-Count* response header with the number of results that would be returned if *limit* and *offset* were ignored.  Since requesting a total count can have a performance impact, it is recommended not to send **count=true** if that value will not be used.  See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to false)
        $filters =  # string | Filter results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#filtering-results)  Filtering is supported for the following fields and operators:  **id**: *eq, ne*  **name**: *eq, ne*  **priority**: *eq, ne* (optional) (default to undefined)
        $sorters =  # string | Sort results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#sorting-results)  Sorting is supported for the following fields: **id, name, priority** (optional) (default to undefined)
        
        # Export Identity Profiles
        
        try {
            
            
            # Below is a request that includes all optional parameters
            #  -Betalimit $limit -Betaoffset $offset -Betacount $count -Betafilters $filters -Betasorters $sorters  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles/{identity-profile-id}/default-identity-attribute-config
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $identityProfileId =  # string | The Identity Profile ID (default to undefined)
        
        # Default identity attribute config
        
        try {
            -BetaidentityProfileId $identityProfileId 
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityProfileId $identityProfileId  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles/{identity-profile-id}
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $identityProfileId =  # string | The Identity Profile ID (default to undefined)
        
        # Gets a single Identity Profile
        
        try {
            -BetaidentityProfileId $identityProfileId 
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityProfileId $identityProfileId  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles/import
  method: POST
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        # Array<IdentityProfileExportedObjectBeta> | Previously exported Identity Profiles.
        $identityProfileExportedObjectBeta = @""@ 
        
        
        # Import Identity Profiles
        
        try {
            $Result = ConvertFrom-JsonToidentityProfileExportedObjectBeta -Json $identityProfileExportedObjectBeta
            -BetaidentityProfileExportedObjectBeta $Result
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityProfileExportedObjectBeta $identityProfileExportedObjectBeta  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $limit =  # number | Max number of results to return. See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to 250)
        $offset =  # number | Offset into the full result set. Usually specified with *limit* to paginate through the results. See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to 0)
        $count =  # boolean | If *true* it will populate the *X-Total-Count* response header with the number of results that would be returned if *limit* and *offset* were ignored.  Since requesting a total count can have a performance impact, it is recommended not to send **count=true** if that value will not be used.  See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to false)
        $filters =  # string | Filter results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#filtering-results)  Filtering is supported for the following fields and operators:  **id**: *eq, ne, ge, gt, in, le, lt, isnull, sw*  **name**: *eq, ne, in, le, lt, isnull, sw*  **priority**: *eq, ne* (optional) (default to undefined)
        $sorters =  # string | Sort results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#sorting-results)  Sorting is supported for the following fields: **id, name, priority, created, modified, owner.id, owner.name** (optional) (default to undefined)
        
        # Identity Profiles List
        
        try {
            
            
            # Below is a request that includes all optional parameters
            #  -Betalimit $limit -Betaoffset $offset -Betacount $count -Betafilters $filters -Betasorters $sorters  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles/identity-preview
  method: POST
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $identityPreviewRequestBeta = @"
        
        "@
        
        # Generate Identity Profile Preview
        
        try {
            $Result = ConvertFrom-JsonToidentityPreviewRequestBeta -Json $identityPreviewRequestBeta
            -BetaidentityPreviewRequestBeta $Result
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityPreviewRequestBeta $identityPreviewRequestBeta  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles/{identity-profile-id}/process-identities
  method: POST
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $identityProfileId =  # string | The Identity Profile ID to be processed (default to undefined)
        
        # Process identities under profile
        
        try {
            -BetaidentityProfileId $identityProfileId 
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityProfileId $identityProfileId  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
- path: /identity-profiles/{identity-profile-id}
  method: PATCH
  xCodeSample:
    - lang: TypeScript
      label: SDK
      source: |
        $identityProfileId =  # string | The Identity Profile ID (default to undefined)
        # Array<JsonPatchOperationBeta> | A list of Identity Profile update operations according to the [JSON Patch](https://tools.ietf.org/html/rfc6902) standard.
        $jsonPatchOperationBeta = @"[{op=add, path=/identityAttributeConfig/attributeTransforms/0, value={identityAttributeName=location, transformDefinition={type=accountAttribute, attributes={sourceName=Employees, attributeName=location, sourceId=2c91808878b7d63b0178c66ffcdc4ce4}}}}]"@ 
        
        
        # Update the Identity Profile
        
        try {
            $Result = ConvertFrom-JsonTojsonPatchOperationBeta -Json $jsonPatchOperationBeta
            -BetaidentityProfileId $identityProfileId -BetajsonPatchOperationBeta $Result
            
            # Below is a request that includes all optional parameters
            #  -BetaidentityProfileId $identityProfileId -BetajsonPatchOperationBeta $jsonPatchOperationBeta  
        } catch {
            Write-Host $_.Exception.Response.StatusCode.value__ "Exception occurred when calling "
            Write-Host $_.ErrorDetails
        }
