- path: /intelligence/v1/response-actions
  method: POST
  xCodeSample:
    - lang: TypeScript
      label: SDK_tools/sdk/typescript/intelligence/methods/intelligence#create-response-action-v1
      source: |
        import { IntelligenceApi } from '@sailpoint/api-client';
        import { Configuration } from '@sailpoint/api-client';
        import { Responseactioncreaterequest } from '@sailpoint/api-client/dist/intelligence/api';

        const configuration = new Configuration();
        const apiInstance = new IntelligenceApi(configuration);
        const responseactioncreaterequest: Responseactioncreaterequest = {
  "actionType" : "DISABLE_ACCOUNT",
  "identityType" : "HUMAN",
  "identityId" : "2c918085842e69ae018428c919680149",
  "accountIds" : [ "2c918085abc000000000000000000001" ],
  "context" : {
    "reason" : "Contain compromised account",
    "externalAlertId" : "CS-FALCON-12345",
    "source" : "CROWDSTRIKE",
    "operator" : "soc-analyst@customer.com"
  }
}; // Responseactioncreaterequest | 
        const result = await apiInstance.createResponseActionV1({ responseactioncreaterequest: responseactioncreaterequest });
        console.log(result);
- path: /intelligence/v1/identities
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK_tools/sdk/typescript/intelligence/methods/intelligence#get-identity-intelligence-v1
      source: |
        import { IntelligenceApi } from '@sailpoint/api-client';
        import { Configuration } from '@sailpoint/api-client';

        const configuration = new Configuration();
        const apiInstance = new IntelligenceApi(configuration);
        const filters: string = 'id eq "ef38f94347e94562b5bb8424a56397d8"'; // string | Filter results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#filtering-results)  Filtering is supported for the following fields and operators:  **id**: *eq*  **email**: *eq*  **opaqueIdentifier**: *eq* (default to undefined)
        const result = await apiInstance.getIdentityIntelligenceV1({ filters: filters });
        console.log(result);
- path: /intelligence/v1/identities/{id}/access-history/access-items
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK_tools/sdk/typescript/intelligence/methods/intelligence#get-intel-identity-access-item-history-v1
      source: |
        import { IntelligenceApi } from '@sailpoint/api-client';
        import { Configuration } from '@sailpoint/api-client';

        const configuration = new Configuration();
        const apiInstance = new IntelligenceApi(configuration);
        const id: string = 'ef38f94347e94562b5bb8424a56397d8'; // string | Non-empty identity id path segment for Intelligence sub-resources. (default to undefined)
        const limit: number = 250; // number | Page size. Defaults to 250; values above 250 are rejected with 400. (optional) (default to 250)
        const offset: number = 0; // number | Zero-based page offset. Defaults to 0. (optional) (default to 0)
        const count: boolean = true; // boolean | If *true* it will populate the *X-Total-Count* response header with the number of results that would be returned if *limit* and *offset* were ignored.  Since requesting a total count can have a performance impact, it is recommended not to send **count=true** if that value will not be used.  See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to false)
        const result = await apiInstance.getIntelIdentityAccessItemHistoryV1({ id: id });
        console.log(result);
- path: /intelligence/v1/identities/{id}/accounts
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK_tools/sdk/typescript/intelligence/methods/intelligence#get-intel-identity-accounts-v1
      source: |
        import { IntelligenceApi } from '@sailpoint/api-client';
        import { Configuration } from '@sailpoint/api-client';

        const configuration = new Configuration();
        const apiInstance = new IntelligenceApi(configuration);
        const id: string = 'ef38f94347e94562b5bb8424a56397d8'; // string | Non-empty identity id path segment for Intelligence sub-resources. (default to undefined)
        const limit: number = 250; // number | Page size. Defaults to 250; values above 250 are rejected with 400. (optional) (default to 250)
        const offset: number = 0; // number | Zero-based page offset. Defaults to 0. (optional) (default to 0)
        const count: boolean = true; // boolean | If *true* it will populate the *X-Total-Count* response header with the number of results that would be returned if *limit* and *offset* were ignored.  Since requesting a total count can have a performance impact, it is recommended not to send **count=true** if that value will not be used.  See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to false)
        const result = await apiInstance.getIntelIdentityAccountsV1({ id: id });
        console.log(result);
- path: /intelligence/v1/identities/{id}/access-history/certifications
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK_tools/sdk/typescript/intelligence/methods/intelligence#get-intel-identity-certification-history-v1
      source: |
        import { IntelligenceApi } from '@sailpoint/api-client';
        import { Configuration } from '@sailpoint/api-client';

        const configuration = new Configuration();
        const apiInstance = new IntelligenceApi(configuration);
        const id: string = 'ef38f94347e94562b5bb8424a56397d8'; // string | Non-empty identity id path segment for Intelligence sub-resources. (default to undefined)
        const limit: number = 250; // number | Page size. Defaults to 250; values above 250 are rejected with 400. (optional) (default to 250)
        const offset: number = 0; // number | Zero-based page offset. Defaults to 0. (optional) (default to 0)
        const count: boolean = true; // boolean | If *true* it will populate the *X-Total-Count* response header with the number of results that would be returned if *limit* and *offset* were ignored.  Since requesting a total count can have a performance impact, it is recommended not to send **count=true** if that value will not be used.  See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to false)
        const result = await apiInstance.getIntelIdentityCertificationHistoryV1({ id: id });
        console.log(result);
- path: /intelligence/v1/identities/{id}/non-human-identity-ownership/{category}
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK_tools/sdk/typescript/intelligence/methods/intelligence#get-intel-identity-non-human-identity-ownership-v1
      source: |
        import { IntelligenceApi } from '@sailpoint/api-client';
        import { Configuration } from '@sailpoint/api-client';

        const configuration = new Configuration();
        const apiInstance = new IntelligenceApi(configuration);
        const id: string = 'ef38f94347e94562b5bb8424a56397d8'; // string | Non-empty identity id path segment for Intelligence sub-resources. (default to undefined)
        const category: string = 'agents'; // 'agents' | 'applications' | Non-human identity ownership category. Use `agents` for AI Agent subtypes and `applications` for Application subtypes.  (default to undefined)
        const ownershipRole: string = 'primary'; // 'primary' | 'secondary' | Optional ownership role discriminator. When set to `primary` or `secondary`, returns one paged role bucket. When omitted, defaults to `primary`.  (optional) (default to 'primary')
        const limit: number = 250; // number | Page size. Defaults to 250; values above 250 are rejected with 400. (optional) (default to 250)
        const offset: number = 0; // number | Zero-based page offset. Defaults to 0. (optional) (default to 0)
        const count: boolean = true; // boolean | If *true* it will populate the *X-Total-Count* response header with the number of results that would be returned if *limit* and *offset* were ignored.  Since requesting a total count can have a performance impact, it is recommended not to send **count=true** if that value will not be used.  See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to false)
        const result = await apiInstance.getIntelIdentityNonHumanIdentityOwnershipV1({ id: id, category: category });
        console.log(result);
- path: /intelligence/v1/identities/{id}/outliers/rare-access
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK_tools/sdk/typescript/intelligence/methods/intelligence#get-intel-identity-rare-access-v1
      source: |
        import { IntelligenceApi } from '@sailpoint/api-client';
        import { Configuration } from '@sailpoint/api-client';

        const configuration = new Configuration();
        const apiInstance = new IntelligenceApi(configuration);
        const id: string = 'ef38f94347e94562b5bb8424a56397d8'; // string | Non-empty identity id path segment for Intelligence sub-resources. (default to undefined)
        const limit: number = 250; // number | Page size. Defaults to 250; values above 250 are rejected with 400. (optional) (default to 250)
        const offset: number = 0; // number | Zero-based page offset. Defaults to 0. (optional) (default to 0)
        const count: boolean = true; // boolean | If *true* it will populate the *X-Total-Count* response header with the number of results that would be returned if *limit* and *offset* were ignored.  Since requesting a total count can have a performance impact, it is recommended not to send **count=true** if that value will not be used.  See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information. (optional) (default to false)
        const result = await apiInstance.getIntelIdentityRareAccessV1({ id: id });
        console.log(result);
- path: /intelligence/v1/response-actions/{id}/status
  method: GET
  xCodeSample:
    - lang: TypeScript
      label: SDK_tools/sdk/typescript/intelligence/methods/intelligence#get-response-action-status-v1
      source: |
        import { IntelligenceApi } from '@sailpoint/api-client';
        import { Configuration } from '@sailpoint/api-client';

        const configuration = new Configuration();
        const apiInstance = new IntelligenceApi(configuration);
        const id: string = '3f1e6c9a-8b2d-4e5f-9a1b-2c3d4e5f6a7b'; // string | The requestId of the response action to look up. (default to undefined)
        const result = await apiInstance.getResponseActionStatusV1({ id: id });
        console.log(result);
